{
  "name": "emacs-bridge",
  "version": "1.0.0",
  "description": "Claude Code plugin to push events to Emacs via Unix socket",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.42",
    "@types/node": "^20.0.0",
    "bonjour-service": "^1.3.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^4.0.18"
  }
}
